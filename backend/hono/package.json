{
	"name": "hono",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"lint": "biome format --write",
		"p:migrate": "bunx --bun prisma migrate dev",
		"p:generate": "bunx --bun prisma generate",
		"p:migrate:reset": "bunx --bun prisma migrate reset"
	},
	"dependencies": {
		"@prisma/adapter-mariadb": "^7.2.0",
		"@prisma/client": "^7.2.0",
		"hono": "^4.11.1",
		"reflect-metadata": "^0.2.2",
		"typedi": "^0.10.0"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.10",
		"@types/bun": "latest",
		"prisma": "^7.2.0"
	}
}